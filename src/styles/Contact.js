import styled from 'styled-components';

const ContactContainer = styled.div`
  .contact-section {
    min-height: 100vh;

    div {
      text-align: center;
    }
  }
`;

export { ContactContainer };